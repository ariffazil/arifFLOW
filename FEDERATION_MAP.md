# FEDERATION_MAP.md — arifFLOW

```yaml
layer: L1
role: GOVERNANCE
function: Federated Intelligence Flow Layer
status: ACTIVE
canon: (internal — sistem saraf)

identity:
  repository: ariffazil/arifFLOW
  organ: arifFLOW Nervous System
  language: Rust (edition 2024)
  tests: 82/82 passing
  version: v2026.07.25

function: |
  arifFLOW is the Federated Intelligence Flow Layer — the "sistem saraf"
  (nervous system) of the arifOS Federation. It sits between LAW and HANDS:

  arifOS (law/Python) ──adjudicate──▶ arifFLOW (flow/Rust) ──schedule──▶ A-FORGE (hands/TypeScript)

  It is a governed parallel execution engine: BSP scheduler, Merkle-anchored
  state transitions, TRI_WITNESS merges, Flow Quotient (FQ) monitoring,
  and metabolic closure on every run.

  arifFLOW schedules, channels, checkpoints, and records.
  It does NOT judge. It does NOT seal. It does NOT authorize.
  Every super-step waits for arifOS verdict.

  Nothing moves without FLOW. FLOW does not create law. FLOW does not own organs.
  FLOW transmits governed intelligence.

upstream:
  - ariffazil/arifos       # L0 — constitutional kernel (verdict authority)
  - ariffazil/ariffazil    # L0 — human sovereign

infra_flow_components:
  interaction:
    - ariffazil/AAA              # cockpit, A2A gateway
  judgment_routing:
    - ariffazil/APEX             # 888 review routing
  execution:
    - ariffazil/A-FORGE          # hands — deploy, mutate, build
  service:
    - ariffazil/compose          # container composition
  registry:
    - ariffazil/web-canon        # navigation, route registry
  surface:
    - ariffazil/arif-fazil.com   # public projection
  search:
    - ariffazil/searxng          # discovery flow
  model:
    - ariffazil/arifOS-model-registry  # model provenance
  analytics:
    - ariffazil/SAF              # SPSS/statistics flow
  benchmark:
    - ariffazil/A2B              # evaluation flow
  audit:
    - ariffazil/EEE              # spine health, recovery
  capability:
    - ariffazil/awesome-mcp-servers  # tool capability catalog
  inference:
    - FLAME                       # free-tier inference router
  observability:
    - Kabarkan                    # span emission, FQ telemetry

downstream_organs:
  - ariffazil/geox         # L2 — earth intelligence
  - ariffazil/wealth       # L2 — capital intelligence
  - ariffazil/well         # L2 — human readiness
  - ariffazil/HERMES       # L2 — multi-modal bridge

vault_relationship:
  vault_authority: ARIFFAZIL / arifOS
  vault_writer: arifFLOW adapter (writes checkpoint receipts, does NOT own VAULT999)

zen: |
  ARIF menentukan.
  arifOS menghukum.
  ATLAS333 berfikir.
  arifFLOW mengalirkan.
  A-FORGE melaksanakan.
  Organs membaca realiti.
  VAULT999 menyimpan saksi.
```

**DITEMPA BUKAN DIBERI — Forged, Not Given.**
